import{_ as a,E as n,H as e,ax as i}from"./chunks/framework.Dqz8snQr.js";const u=JSON.parse('{"title":"常见问题","description":"","frontmatter":{},"headers":[],"relativePath":"QUESTIONS.md","filePath":"QUESTIONS.md","lastUpdated":1781594991000}'),l={name:"QUESTIONS.md"};function o(t,s,d,p,c,r){return e(),n("div",null,s[0]||(s[0]=[i(`<h1 id="常见问题" tabindex="-1">常见问题 <a class="header-anchor" href="#常见问题" aria-label="Permalink to &quot;常见问题&quot;">​</a></h1><h2 id="单文件导入" tabindex="-1">单文件导入 <a class="header-anchor" href="#单文件导入" aria-label="Permalink to &quot;单文件导入&quot;">​</a></h2><p>单文件引入就是不从入口文件引入，而是从子目录引入：</p><div class="language-diff vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">diff</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#B31D28;--shiki-dark:#FDAEB7;">- import { flat } from &#39;t-comm&#39;;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">+ import { flat } from &#39;t-comm/es/list/index&#39;;</span></span></code></pre></div><p>有两种情况需要从单文件引入</p><ol><li>前端工程。由于 <code>t-comm</code> 中使用了 <code>nodejs</code> 包，在前端项目中直接引入 <code>t-comm</code> 会因为这些依赖而报错</li><li>想要 <code>tress-shaking</code>，但是业务项目不支持，比如 <code>Vue2.x</code> 版本的 Uni App 等项目</li></ol><h2 id="es-目录和-lib-目录的区别" tabindex="-1">es 目录和 lib 目录的区别 <a class="header-anchor" href="#es-目录和-lib-目录的区别" aria-label="Permalink to &quot;es 目录和 lib 目录的区别&quot;">​</a></h2><ul><li><code>es</code> 导出的内容属于 <a href="https://nodejs.org/api/esm.html" target="_blank" rel="noreferrer">ESM</a> 规范</li><li><code>lib</code> 目录导出的内容数据 <a href="https://nodejs.org/api/modules.html" target="_blank" rel="noreferrer">CommonJS</a> 规范</li></ul><h2 id="导出目录结构" tabindex="-1">导出目录结构 <a class="header-anchor" href="#导出目录结构" aria-label="Permalink to &quot;导出目录结构&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>t-comm/</span></span>
<span class="line"><span>├── bin/                  # CLI 命令入口文件</span></span>
<span class="line"><span>│   └── cli.js            # CLI 主程序</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├── es/                   # ESM 模块构建产物（Rollup Config 1 输出）</span></span>
<span class="line"><span>│   ├── list/</span></span>
<span class="line"><span>│   │   ├── index.mjs     # 模块入口（注意扩展名为 .mjs）</span></span>
<span class="line"><span>│   │   └── flat.mjs      # 具体实现</span></span>
<span class="line"><span>│   ├── debounce/</span></span>
<span class="line"><span>│   │   └── debounce.mjs</span></span>
<span class="line"><span>│   ├── index.mjs         # ESM 聚合入口（re-export 所有子模块）</span></span>
<span class="line"><span>│   ├── index.d.ts        # 类型声明（tsconfig.es.json 生成）</span></span>
<span class="line"><span>│   └── *.js              # Rollup 提取的公共 chunk</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├── lib/                  # CJS 模块构建产物（Rollup Config 0 输出）</span></span>
<span class="line"><span>│   ├── list/</span></span>
<span class="line"><span>│   │   ├── index.js      # 模块入口</span></span>
<span class="line"><span>│   │   └── flat.js       # 具体实现</span></span>
<span class="line"><span>│   ├── debounce/</span></span>
<span class="line"><span>│   │   └── debounce.js</span></span>
<span class="line"><span>│   ├── index.js          # CJS 聚合入口（package.json main 字段）</span></span>
<span class="line"><span>│   ├── index.esm.js      # ESM 聚合入口（package.json module 字段，Rollup Config 2 输出）</span></span>
<span class="line"><span>│   ├── index.d.ts        # 类型声明入口（package.json typings 字段，API Extractor 合并产物）</span></span>
<span class="line"><span>│   └── *.js              # Rollup 提取的公共 chunk</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└── README.md             # 项目说明文档</span></span></code></pre></div><h2 id="构建产物分析" tabindex="-1">构建产物分析 <a class="header-anchor" href="#构建产物分析" aria-label="Permalink to &quot;构建产物分析&quot;">​</a></h2><h3 id="构建流程" tabindex="-1">构建流程 <a class="header-anchor" href="#构建流程" aria-label="Permalink to &quot;构建流程&quot;">​</a></h3><p>构建由 Gulp + Rollup 驱动，执行 <code>npm run build</code> 时依次完成：</p><ol><li><strong>类型检查</strong>：<code>tsc --noEmit --skipLibCheck</code>（<code>package.json</code> 的 <code>type-check</code> 脚本）</li><li><strong>清理旧产物</strong>：同时删除 <code>lib/</code> 和 <code>es/</code> 目录（<code>clearLibFile</code> 任务）</li><li><strong>Rollup 打包</strong>：按三个 config 依次构建（<code>buildByRollup</code> 任务）</li><li><strong>生成声明文件</strong>：API Extractor 合并 <code>.d.ts</code> 为 <code>lib/index.d.ts</code>（<code>generateDts</code> 任务）</li></ol><h3 id="rollup-三个-config-的分工" tabindex="-1">Rollup 三个 Config 的分工 <a class="header-anchor" href="#rollup-三个-config-的分工" aria-label="Permalink to &quot;Rollup 三个 Config 的分工&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Config</th><th>输入</th><th>输出</th><th>格式</th><th>说明</th></tr></thead><tbody><tr><td>Config 0</td><td>所有 <code>src/**/*.ts</code>（多入口）</td><td><code>lib/</code></td><td>CJS</td><td>生成各子模块的 <code>.js</code> 文件，<code>declaration: false</code>，使用 <code>tsconfig.json</code></td></tr><tr><td>Config 1</td><td>所有 <code>src/**/*.ts</code>（多入口）</td><td><code>es/</code></td><td>ESM</td><td>生成各子模块的 <code>.mjs</code> 文件（<code>entryFileNames: &#39;[name].mjs&#39;</code>），使用 <code>tsconfig.es.json</code></td></tr><tr><td>Config 2</td><td><code>src/index.ts</code>（单入口）</td><td><code>lib/index.esm.js</code></td><td>ESM</td><td>生成聚合入口，<code>declaration: true</code> 输出分散 <code>.d.ts</code>，使用 <code>tsconfig.build.json</code></td></tr></tbody></table><h3 id="产物文件说明" tabindex="-1">产物文件说明 <a class="header-anchor" href="#产物文件说明" aria-label="Permalink to &quot;产物文件说明&quot;">​</a></h3><p><strong><code>lib/</code> 目录（CJS，<code>package.json</code> 的 <code>main</code> 字段）</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lib/</span></span>
<span class="line"><span>├── index.js          # CJS 聚合入口（package.json main 字段）</span></span>
<span class="line"><span>├── index.esm.js      # ESM 聚合入口（package.json module 字段）</span></span>
<span class="line"><span>├── index.d.ts        # 类型声明入口（package.json typings 字段，API Extractor 合并产物）</span></span>
<span class="line"><span>├── &lt;module&gt;/         # 各子模块目录，每个对应 src/ 下的一个模块</span></span>
<span class="line"><span>│   ├── index.js</span></span>
<span class="line"><span>│   └── *.js</span></span>
<span class="line"><span>└── *.js              # 公共 chunk（rollup 提取的共享代码）</span></span></code></pre></div><p><strong><code>es/</code> 目录（ESM，支持 Tree Shaking）</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>es/</span></span>
<span class="line"><span>├── index.mjs         # ESM 聚合入口，re-export 所有子模块</span></span>
<span class="line"><span>├── index.d.ts        # 类型声明（tsconfig.es.json 生成）</span></span>
<span class="line"><span>├── &lt;module&gt;/         # 各子模块目录</span></span>
<span class="line"><span>│   └── *.mjs</span></span>
<span class="line"><span>└── *.js              # 公共 chunk</span></span></code></pre></div><h3 id="es-index-mjs-的特点" tabindex="-1"><code>es/index.mjs</code> 的特点 <a class="header-anchor" href="#es-index-mjs-的特点" aria-label="Permalink to &quot;\`es/index.mjs\` 的特点&quot;">​</a></h3><ul><li>以具名导出（named export）方式聚合所有子模块，共导出 <strong>100+ 个模块</strong></li><li>每个导出精确到具体函数，例如：<div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { flat, flatten } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./list/flat.mjs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { debounce, debounceRun } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./debounce/debounce.mjs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div></li><li>末尾有部分 <code>import</code> 语句（无导出），用于执行副作用模块的初始化</li></ul><h3 id="lib-index-d-ts-的生成过程" tabindex="-1"><code>lib/index.d.ts</code> 的生成过程 <a class="header-anchor" href="#lib-index-d-ts-的生成过程" aria-label="Permalink to &quot;\`lib/index.d.ts\` 的生成过程&quot;">​</a></h3><ol><li>Rollup Config 2（单入口）的 <code>@rollup/plugin-typescript</code> 先生成分散的 <code>.d.ts</code></li><li>API Extractor 读取 <code>lib/index.d.ts</code> 作为入口，将所有类型合并为单一文件</li><li>清理 <code>lib/</code> 下多余的 <code>.d.ts</code> 文件，只保留 <code>index.d.ts</code></li></ol><blockquote><p>⚠️ 注意：<code>tsconfig.json</code> 的 <code>include</code> 不能包含 <code>test</code> 目录，否则测试文件的类型会干扰 <code>.d.ts</code> 的生成，导致 <code>lib/index.d.ts</code> 无法正常输出。构建专用配置见 <code>tsconfig.build.json</code>。</p></blockquote>`,26)]))}const g=a(l,[["render",o]]);export{u as __pageData,g as default};
